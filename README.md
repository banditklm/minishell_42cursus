# minishell_42cursus
A simple Unix shell clone built in C, featuring a custom prompt, built-in commands (cd, pwd, exit, etc.), environment variable management, redirections, pipes, and signal handling. Adheres to POSIX standards with no external libraries, focusing on parsing, process management, and error handling.
